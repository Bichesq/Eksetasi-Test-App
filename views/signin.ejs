<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome - Sign In</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
        background-color: var(--primary-color);
        background-image: linear-gradient(135deg, var(--primary-color) 0%, #FFD54F 100%);
    }
    .signin-wrapper {
        background: white;
        min-height: 100vh;
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        position: relative;
        overflow: hidden;
    }
    /* Simulate the white curved bottom sheet look */
    .signin-card {
        width: 100%;
        max-width: 380px;
        padding: 40px 30px;
        z-index: 2;
    }
    .decoration-circle {
        position: absolute;
        top: -100px;
        right: -100px;
        width: 300px;
        height: 300px;
        background: rgba(255, 193, 7, 0.1);
        border-radius: 50%;
        z-index: 1;
    }
    .illustration-placeholder {
        text-align: center;
        margin-bottom: 2rem;
    }
    .illustration-placeholder img {
        max-width: 120px;
        opacity: 0.8;
    }
    .signin-footer {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 15vh;
        background: var(--primary-color);
        clip-path: polygon(0 40%, 100% 0, 100% 100%, 0% 100%);
        z-index: 1;
    }
    h2 { font-size: 1.8rem; margin-bottom: 0.5rem; }
    p.subtitle { color: #666; margin-bottom: 2rem; }
  </style>
</head>
<body>

<div class="signin-wrapper">
    <div class="decoration-circle"></div>
    
    <div class="signin-card">
        <div class="illustration-placeholder">
             <!-- Placeholder for an icon/logo similar to the burger/drink in reference -->
             <div style="font-size: 3rem;">🍔</div>
        </div>

        <h2>Welcome!</h2>
        <p class="subtitle">Sign in to Continue</p>
  
        <% if (error) { %>
            <div class="alert alert-error"><%= error %></div>
        <% } %>

        <form action="/signin" method="POST">
            <div class="form-group">
            <input type="text" id="username" name="username" required placeholder="Name / Username" value="<%= locals.form?.username || 'testuser' %>">
            </div>
            
            <div class="form-group">
            <input type="password" id="password" name="password" required placeholder="Password" value="<%= locals.form?.password || 'password123' %>">
            </div>
            
            <div class="form-group">
            <input type="text" id="apiKey" name="apiKey" required placeholder="API Key" value="<%= locals.apiKey || '' %>">
            </div>

            <button type="submit">
                Access Account <span>&rarr;</span>
            </button>
        </form>

        <div style="margin-top: 2rem; text-align: center;">
             <label class="checkbox-field" style="justify-content: center; font-size: 0.8rem; color: #888;">
                <input type="checkbox" checked disabled> I agree with Terms & Conditions!
             </label>
        </div>
        
        <div style="margin-top: 2rem; text-align: center; font-size: 0.8rem; color: #ccc;">
            Test Credentials: testuser / password123
        </div>
    </div>
    
    <div class="signin-footer"></div>
</div>

</body>
</html>
