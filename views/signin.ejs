<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign In - Test App</title>
  <style>
    body { font-family: sans-serif; max-width: 400px; margin: 40px auto; padding: 20px; }
    .form-group { margin-bottom: 15px; }
    label { display: block; margin-bottom: 5px; font-weight: bold; }
    input { width: 100%; padding: 8px; box-sizing: border-box; }
    button { padding: 10px 20px; background: #007bff; color: white; border: none; cursor: pointer; width: 100%; }
    button:hover { background: #0056b3; }
    .error { color: red; background: #ffe6e6; padding: 10px; margin-bottom: 15px; border: 1px solid red; }
    .info { color: #666; font-size: 0.9em; margin-top: 20px; text-align: center; }
  </style>
</head>
<body>
  <h2>Sign In</h2>
  
  <% if (error) { %>
    <div class="error"><%= error %></div>
  <% } %>

  <form action="/signin" method="POST">
    <div class="form-group">
      <label for="username">Username</label>
      <input type="text" id="username" name="username" required value="<%= locals.form?.username || 'testuser' %>">
    </div>
    
    <div class="form-group">
      <label for="password">Password</label>
      <input type="password" id="password" name="password" required value="<%= locals.form?.password || 'password123' %>">
    </div>
    
    <div class="form-group">
      <label for="apiKey">API Key (X-API-Key)</label>
      <input type="text" id="apiKey" name="apiKey" required value="<%= locals.apiKey || '' %>" placeholder="Enter valid API Key">
    </div>

    <button type="submit">Sign In</button>
  </form>

  <div class="info">
    <p>Test Credentials: testuser / password123</p>
  </div>
</body>
</html>
